{
    "config": {
        "step": {
            "user": {
                "title": "Add a Family Calendar Sync",
                "description": "Choose a destination, then choose source calendars to sync in full and/or source calendars to filter by title.",
                "data": {
                    "to_entity_id": "Sync to calendar",
                    "copy_all_from": "Sync every event from these calendar(s)",
                    "from_entities": "Sync matching events from these calendar(s)",
                    "keywords": "Match these words or phrases in the event title",
                    "ignore_event_if_title_starts_with": "Do not sync events whose title starts with",
                    "days_to_sync": "Days ahead to sync",
                    "days_to_sync_past": "Days in the past to sync",
                    "sync_interval_minutes": "Sync every (minutes)"
                },
                "data_description": {
                    "copy_all_from": "Every event from these source calendars is copied. Do not also select the same calendar under filtered sync.",
                    "from_entities": "Only events whose title matches a word or phrase below are copied from these calendars.",
                    "keywords": "Required when using filtered sync. Matching is case-insensitive and uses whole words/phrases in the event title only; descriptions are not searched.",
                    "ignore_event_if_title_starts_with": "Applied before all other rules, to every source calendar. This prefix check is case-sensitive. For example, use ! to exclude !Private event.",
                    "days_to_sync": "Copy the whole current day plus this many full calendar days ahead.",
                    "days_to_sync_past": "Add this many complete calendar days before today. A value of 0 still includes all events from today."
                }
            }
        },
        "error": {
            "no_source_calendars_selected": "Choose at least one source calendar to sync.",
            "to_cannot_be_from": "The destination calendar can't also be a source calendar.",
            "calendar_in_both_sync_modes": "Choose each source calendar in either full sync or filtered sync, not both.",
            "keywords_required_for_matching_calendars": "Add at least one title keyword or phrase for the filtered source calendars."
        },
        "abort": {
            "already_configured": "This calendar already has a Family Calendar Sync configured."
        }
    },
    "options": {
        "step": {
            "init": {
                "title": "Family Calendar Sync settings",
                "description": "Editing sync settings for {to_entity_id}. Choose full-sync and filtered sources separately.",
                "data": {
                    "copy_all_from": "Sync every event from these calendar(s)",
                    "from_entities": "Sync matching events from these calendar(s)",
                    "keywords": "Match these words or phrases in the event title",
                    "ignore_event_if_title_starts_with": "Do not sync events whose title starts with",
                    "days_to_sync": "Days ahead to sync",
                    "days_to_sync_past": "Days in the past to sync",
                    "sync_interval_minutes": "Sync every (minutes)"
                }
                ,"data_description": {
                    "copy_all_from": "Every event from these source calendars is copied. Do not also select the same calendar under filtered sync.",
                    "from_entities": "Only events whose title matches a word or phrase below are copied from these calendars.",
                    "keywords": "Required when using filtered sync. Matching is case-insensitive and uses whole words/phrases in the event title only; descriptions are not searched.",
                    "ignore_event_if_title_starts_with": "Applied before all other rules, to every source calendar. This prefix check is case-sensitive.",
                    "days_to_sync": "Copy the whole current day plus this many full calendar days ahead.",
                    "days_to_sync_past": "Add this many complete calendar days before today. A value of 0 still includes all events from today."
                }
            }
        },
        "error": {
            "no_source_calendars_selected": "Choose at least one source calendar to sync.",
            "to_cannot_be_from": "The destination calendar can't also be a source calendar.",
            "calendar_in_both_sync_modes": "Choose each source calendar in either full sync or filtered sync, not both.",
            "keywords_required_for_matching_calendars": "Add at least one title keyword or phrase for the filtered source calendars."
        }
    },
    "entity": {
        "sensor": {
            "last_sync": {
                "name": "Last sync"
            }
        },
        "button": {
            "sync_now": {
                "name": "Sync now"
            }
        }
    },
    "issues": {
        "yaml_no_longer_supported": {
            "title": "Family Calendar Sync no longer supports YAML configuration",
            "description": "Family Calendar Sync is now configured entirely through the UI.\n\nGo to **Settings -> Devices & Services -> Add Integration -> Family Calendar Sync** to recreate your sync(s), then remove the `family_calendar_sync:` section from `configuration.yaml`."
        }
    },
    "services": {
        "sync": {
            "name": "Sync now",
            "description": "Triggers an immediate sync for one Family Calendar Sync entry, or all of them if none is specified.",
            "fields": {
                "config_entry_id": {
                    "name": "Config entry ID",
                    "description": "The config entry ID to sync. Leave empty to sync all configured entries."
                }
            }
        }
    }
}
